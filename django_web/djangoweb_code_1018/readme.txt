完成爬蟲api，但email存進那部份還沒用好。
其他api還沒用喔，注意別被覆蓋到了。

1. 在本地端下載pipenv，在cmd輸入「pip install pipenv」
2. 進入cmd，並路徑要切在musicmain資料夾，在做第三步驟
3. 輸入「pipenv shell」
4. pipenv install -r requirements.txt 第一次進入並安裝套件

exit 離開虛擬環境
pipenv freeze > requirements.txt 輸出套件並儲存

requirements.txt 這是我電腦的本地端的環境，若要統一環境可以下載pipenv